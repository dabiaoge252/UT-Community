<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
    <!-- 指定父项目 -->
    <parent>
        <groupId>com.quanxiaoha</groupId>
        <artifactId>xiaoha-framework</artifactId>
        <version>${revision}</version>
    </parent>
    <!-- 指定打包方式 -->
    <packaging>jar</packaging>

    <artifactId>xiaoha-spring-boot-starter-biz-context</artifactId>
    <name>${project.artifactId}</name>
    <description>上下文组件</description>

  <dependencies>
      <!-- ====================== 自研基础模块（平台统一提供） ====================== -->
      <dependency>
          <groupId>com.quanxiaoha</groupId>
          <artifactId>xiaoha-common</artifactId>
      </dependency>

      <!-- ====================== 线程上下文传递（TTL，支持线程池场景传递用户信息） ====================== -->
      <dependency>
          <groupId>com.alibaba</groupId>
          <artifactId>transmittable-thread-local</artifactId>
      </dependency>

      <!-- ====================== Spring 核心与 Web（提供过滤器/拦截器运行环境） ====================== -->
      <!-- Spring 核心 -->
      <dependency>
          <groupId>org.springframework.boot</groupId>
          <artifactId>spring-boot-starter</artifactId>
      </dependency>

      <dependency>
          <groupId>org.springframework</groupId>
          <artifactId>spring-web</artifactId>
      </dependency>

      <dependency>
          <groupId>jakarta.servlet</groupId>
          <artifactId>jakarta.servlet-api</artifactId>
      </dependency>

      <!-- ====================== Feign 相关（Feign 请求拦截器透传 userId） ====================== -->
      <dependency>
          <groupId>com.alibaba.cloud</groupId>
          <artifactId>spring-cloud-alibaba-commons</artifactId>
      </dependency>

      <dependency>
          <groupId>io.github.openfeign</groupId>
          <artifactId>feign-core</artifactId>
      </dependency>

  </dependencies>
</project>
